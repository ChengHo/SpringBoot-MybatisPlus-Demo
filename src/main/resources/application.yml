spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/user?useUnicode=true&characterEncoding=UTF8&autoReconnect=true&useSSL=false
    username: root
    password: root
    driver-class-name: com.mysql.jdbc.Driver

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl # 开启SQL语句打印
  mapper-locations: classpath:mapper/*Mapper.xml # 自定义的SQL使用的 mapper 文件位置

server:
  port: 8099
#  servlet:
#    context-path: /api
